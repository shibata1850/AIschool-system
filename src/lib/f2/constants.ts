/**
 * F2 AIチャットの共有定数。
 * クライアント（chat-panel）とサーバー（tutor）の両方がここを参照し、
 * 上限値の二重定義による食い違いを防ぐ（2026-07-03 監査指摘の修正）。
 */
export const QUESTION_LIMIT = 2000;

/**
 * 講師から受講生へ送る一言の上限（S6の介入導線）。
 * プロンプト本文を渡す用途があるため短すぎてはいけない。
 *
 * **ここに置く理由**: S6のUIは `"use client"` のため、DBクライアントを含む
 * `chatLog.ts` を import できない（クライアントバンドルにサーバー専用コードが
 * 混ざって画面ごと壊れる — 2026-09-02 に実際に踏んだ）。
 */
export const TEACHER_MESSAGE_LIMIT = 2000;
