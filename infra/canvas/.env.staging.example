# カスタム層（本リポジトリ）→ ステージングCanvas 接続設定の見本
# 実値は .env に書く（コミット禁止 — CLAUDE.md 2章）。この見本は架空値のみ

# ステージングCanvasのURL（TLS終端後のドメイン。末尾スラッシュなし）
CANVAS_BASE_URL=https://canvas-staging.example.jp

# Canvas管理画面「承認済み連携」で発行したアクセストークン
# 発行手順: docs/Canvasステージング構築手順.md 3.1（有効期限を必ず設定する）
CANVAS_API_TOKEN=
