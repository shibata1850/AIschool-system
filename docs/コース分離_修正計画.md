# コース間の閲覧・操作分離

## 最新の方針（2026-09-14、過去の記述より優先）

- 旧履歴互換対応（ユーザー承認済み）: ホームから `/achievement/history` へ移動し、認証本人IDに完全一致するコース未設定の提出・会話・講師メッセージと旧lesson_recordsを閲覧する。コース指定済み記録は含めず、現在の課題・到達度へ合算しない。講師もこの本人用画面では自分の履歴だけ。表示名やメール一致・所属コース数から本人/コースを推定しない。各分類50件のページ送り、改行保持、講師向け採点根拠の取得禁止。既存行の書換え・移動・削除、新マイグレーションは不要（0012の列追加は別途必要）。旧提出から再提出・採点・Canvas反映を起動する導線は作らない。未完了旧提出の再開は、帰属確認後の別対応とする。
- この追加の確認: 単体/静的画面22件と既存ホーム等4件、計26件PASS。型チェックPASS。実PostgreSQLの所有者/NULL限定・ページング2件、ブラウザの本人限定・URL差替え拒否・改行/横幅・匿名403の1シナリオ×3画面サイズを追加（実行待ち）。以前の644/69合格は追加前の結果であり、新テスト合格とは扱わない。

- 12:59追記: Canvas名簿・成績サマリはCanvasアカウント一覧由来のコース名選択へ接続済み（未起動コースも含む）。選択IDは数値、LTI書込照合とは別経路。既存集計の共有処理を利用。関連55テスト/模擬Canvas4件/型チェック/本番用ローカルビルドPASS。追加E2Eは未実行、実Canvasの一覧権限・本番反映は未確認。

- 11:49追記: モニタリングも全受講生表示に変更。共有座席を維持し、各タイル内でコース別状態を表示する。起動元コース外はメッセージ送信なし。追加/関連22単体・静的描画テストと型チェック成功、ブラウザE2Eは未実施。出席・レポート・Canvas系の画面対応は残る。

- ユーザーの明示指示により、講師・管理者は全コースの詳細を閲覧できる。講師の閲覧を起動元コースだけに制限する旧目標は撤回する。
- 座席は共有運用を維持し、コース別の座席分割は行わない。
- 受講生の本人データ制限、採点・メッセージの送信先照合は維持する。閲覧許可をそのまま全コースへの書込許可には転用しない。
- 会話ログと採点待ち・Canvas未反映一覧に全コース閲覧をローカル実装。採点操作は起動元コースだけに表示し、別コースは閲覧のみ。コース未設定の旧記録も講師一覧には含める。既存の学生向け取得関数と採点APIは変更しない。
- 新しい閲覧関連単体テスト13件、型チェック、差分チェック成功。旧E2Eの会話ログ・採点一覧の期待値を更新したが、更新後E2Eと実DB検証はまだ未実施。他の講師画面への適用・全体検証・本番反映は残る。

2026-09-11。ユーザー承認済み。ローカル実装・検証中。本番未変更。

## 実装経過（11:32更新）

12:19追記: Canvas名簿・成績サマリはコースの先頭選択を廃止。セッション由来コースを直接照合し、両画面でも講師認可を実施。新規再現12件のFAIL確認後に修正。関連183単体/静的描画テスト、模擬Canvas3件、型/差分チェック成功。E2Eは名簿/サマリを含む16件を収集確認のみで未実行。次は固定16名・全体レポート保存・全コース通知をコース別に揃える。本番未変更。

12:08追記: 対応登録API・専用画面をローカル実装。「課題の割当」から開く。LTIコース・講師権限・Originを検証し、登録済み対応は表示のみ。関連148件と模擬Canvasの3件、型/構文/差分チェック成功。ローカル模擬Canvasによる登録・成績送信・拒否・狭幅表示のE2E4件を追加したが、DB統合/E2Eはまだ実行していない。本番未変更。

11:50追記: 演習の送信先を明示登録するcanvas_assignment_linksと0013の候補を追加。コース+演習ごとに1対応、同じコースの別演習によるCanvas課題の重複使用を禁止し、既存対応の変更は行わない。講師のセッションコース内の公開課題・100点満点を検証し、送信時にも尺度を再確認する。確定採点の呼出元まで接続済みだが登録画面/APIは未実装。関連129単体テスト/型チェック成功、対応保存DB統合5件は未実行。本番未変更。

- 修正前のコース分離E2Eはユーザー環境で6失敗/2成功を確認済み。
- コース認可、提出・会話・メッセージ・出席の所属保存と検索条件、モニタリングの分離をローカル実装中。0012の適用は未実施。既存データへの推測バックフィルなし。
- Canvas成績入力はセッションのコースを直接照合し、同コースの公開課題を明示選択する方式へ変更。名簿外の受講生への送信、コースなしの操作は拒否する。課題を一覧の先頭から選ぶ方式はこの経路では使わない。
- 関連単体/画面静的描画テスト109件と型チェックが成功。DB統合・更新後E2E・実画面表示は未検証。
- 演習の確定採点からCanvasへ送るには別途コース別の課題対応づけが必要。現時点の呼出元は未対応で送信スキップになるため、途中版の本番デプロイは禁止。既存のCanvas成績は変更しない。
- 以下の「現在地」は修正前に確認した課題の記録。残る週次集計・通知・到達度・Canvas名簿等と移行設計を完了させてから全体検証する。

## 現在地

- 本番の直近確認HEADは949de27。課題割当・未割当表示・提出者名の3修正は完了済み。
- 課題割当は検証済みLTI courseIdとstudent_coursesを使うが、提出そのものにはcourse_idがない。
- 採点一覧は全提出を対象に状態のみで絞る。採点更新APIは提出IDで取得し、コースを確認しない。
- 会話ログは全学生IDを取得する。メッセージ・出席の更新APIは全体名簿への存在確認のみ。
- submissions / chat_logs / teacher_messages / lesson_recordsにコース所属がない。
- lesson_recordsの主キーはstudent_id・week_start。同じ受講生が同じ週に2コース受講すると分離不能。
- 成績書き戻しのsyncGradeToCanvasはコースを引数に取らずresolveGradebookへ委譲している。アプリの閲覧制限だけでは書込先の安全を保証できない。

これはソース確認結果であり、本番で別コースの個人データを閲覧した証拠ではない。

## 実装方針

1. 検証済みLTIセッションのコースを認可の基準にする。リクエスト本文・クエリのcourseIdは信頼しない。講師ロールだけで全体アクセスを許可しない。
2. 通常の講師画面は管理者で起動しても現在のコースへ限定する。管理者専用の全体監査と、コース内の授業操作を混同しない。
3. 提出・会話・一言・授業記録に所属を保存し、読み取り・更新のSQL条件にも含める。同一受講生の複数所属を名簿フィルタだけで処理しない。
4. 提出の割当重複判定とロックをコース・課題・受講生単位へ合わせる。同じ課題を別コースで受けても混ざらないようにする。
5. 講師の採点・会話ログ・モニタリング・出席・レポートと、受講生のホーム・提出・メッセージ受信・到達度の両端を点検する。
6. Canvas名簿・成績入力・成績サマリ・成績書き戻し・週次通知もコースとの対応を確認する。対応未確定時に最初のコースへ書き込まない。
7. コース不明の本番セッションは拒否する。既存のローカル開発用ロールと本番認証を混同せず、テスト専用経路を本番へ開かない。

## 過去データの移行

- 既存の提出・確定成績・履歴を削除しない。課題の再配布や再採点で置き換えない。
- 新しい列はまず既存データを保持したまま追加する。NULLを全コース共有として扱わない。
- 現在の所属が1件というだけで、過去の全履歴をそのコースへ自動帰属させない。
- 配布時の監査ログに記録されたcourseIdなど、記録単位の根拠を確認する。その他は旧運用の記録・実起動との対応を調べる。
- 未判定の行は保持し、コース別画面から隔離する。移行前後の件数・既存確定スコアの保持を検証する。
- 本番DBの事前調査は件数・有無のみ。LTIのopaque courseIdをCanvasの数値コースIDと同一視しない。
- 一意制約・主キー変更は追加先へのコピーと件数検証を含めて設計する。既存表のDROP/TRUNCATEはしない。

## 回帰テスト

`e2e/regression/2026-09-11-course-isolation.spec.ts` は架空の2コース・2受講生とローカル署名セッションを使う。
実Canvasセッションや本番の署名鍵は使用しない。起動ごとにランダムなテスト署名鍵を生成する。
DB接続は127.0.0.1のaischool_testに限定する。

- 自コースの提出は見えるが他コースの提出は見えない。
- 自コースの会話は見えるが他コースの会話は見えない。
- 自コースへ改行を保持したメッセージを送れる。
- 他コースへのメッセージ・出席・採点操作は拒否し、データ不変。
- コースなしの講師セッションは拒否。
- 自コースでも不正入力は400。

修正前に期待される失敗は6件、正常系2件。ただし実行結果を見ずに再現済みとは扱わない。
続いて同一受講生の複数コース所属、改ざん・学生・匿名、並行配布、週次集計、既存データ移行のテストを追加する。

## 検証・反映の順番

### 本番用ビルドでの隔離負荷測定（2026-09-11追加）

- 開発モードの60件E2Eは機能合格。16名同時操作のp90は画面5.00-13.72秒、提出7.75-30.96秒で目安を超過。開発モードだけが原因とは断定しない。
- 現行E2EのbeforeEachはリセットAPIを使用するため、そのままproductionへ切り替えない。ALLOW_DEV_RESET / DEV_COOKIE_ROLESを有効にして回避しない。
- 専用ハーネスが新しいloopback DBを作り、マイグレーションと架空課題・16名の割当を起動前に投入する。測定中はリセットしない。既存/外部DBには接続しない。
- 同じ候補コードをbuildしてnext startで起動する。APIキーは架空、Canvas/AI接続先はloopback fixture、LTIセッションは実行ごとの生成鍵で署名。本番の資格情報を持ち込まない。
- 計測前にGET/POSTのリセットAPIが無効であることを確認。事前投入・計測・後片付けの責務を分ける。
- 同一画面条件で未計測ウォームアップを行い、測定は複数回・別の提出行を使う。初回とウォームアップ後を区別し、16名の成功件数・本人の保存内容・他コース不変を毎回照合。
- 画面/提出のp50・p90・最大・失敗件数を報告する。ローカルproduction測定と、さくら上の本番性能・実Claude応答時間は別の証拠として扱う。
- 実装・実行は未完。この準備が整うまではユーザーへ同じ開発モード試験の再実行を依頼しない。

1. 修正前E2Eの失敗を確認。
2. データ所属と認可の実装、単体・DB統合・E2Eを合格させる。
3. 既存全テスト、型チェック、ビルド、3条件表示と16クライアント負荷検証。
4. コミット・共有後、SSHをまとめて本番HEAD・バックアップを確認。
5. 移行・ビルド・再起動。DBの件数保持、localhost公開、トップHTTPを確認。
6. Canvasから実LTI起動して自コースの通常操作とコース境界を検証する。既存の確定成績は検証用に上書きしない。

GitHubへのコミットだけを本番反映完了としない。

## 2026-09-11 12:30 週次経路の進捗

- 通知先を明示コースの講師・TAのみに変更。コース未指定は送信しない。通知9件、集計SQL4件は修正前の失敗を確認してから合格。
- 集計元の授業記録/未完了課題にコース条件を追加。旧データは独立したまま保持。DB統合2件追加、未実行。
- 関連単体23ファイル230件、型チェック、差分チェック合格。本番変更なし。
- 未完: コース別の週次保存表、生成処理、管理API、cron、閲覧画面、並行生成/再通知の整合性、DB/E2E検証。途中版の生成関数はコース未指定で通知がスキップされるため、現時点でデプロイ不可。

## 2026-09-11 12:40 週次保存の準備

- 0014でcourse_weekly_reportsを追加する候補を用意（未適用）。旧表は削除/コピーしない。コース+週の主キーと生成識別子を持たせる。
- courseReportStoreでコース必須の保存/取得、生成識別子付きの通知結果更新を実装。新規単体5件合格。DB統合4件は追加のみで未実行。
- 生成/通知の並行制御、保持期限時のpayload処理、生成API/cron/画面への接続が残る。生成識別子の条件は古い結果による上書きを防ぐが、二重送信防止そのものではない。
- 本番変更なし。途中版のためデプロイ禁止を維持。

## 2026-09-11 12:47 生成APIの接続

- 生成処理をコース別名簿・記録・新保存表へ接続し、保存成功後のみ通知する順序へ変更。旧表読み取りは明示コースの新表取得へ移行、コースなしはnull。
- 管理APIは処理前admin/LTIコース/Origin確認、本文のコースは使わない。関連6ファイル41件と型チェック合格。
- cron/閲覧画面はまだ新契約へ未接続。並行送信・再通知・日付・保持期限の整合性、DB/E2Eも未完。途中版を本番へ出さない。

## 2026-09-11 12:55 バッチと対象週

- CLIをLTI記録済みコースごとの逐次生成へ接続。重複/空白コースを除外、失敗は自動再送せず残りを処理しexit1。実際のcron変更・通知実行はしていない。
- 日本時間月曜7時に前週を選ぶ不具合をテストで再現し、生成入口のみAsia/Tokyo基準に変更。実在する月曜の日付検証も追加。
- 関連8ファイル58件、型/差分チェック成功。閲覧画面・別プロセス間の並行送信・保持期限・DB/E2E等は引き続き未完。本番未変更。

## 2026-09-11 13:01 週次閲覧画面

- 閲覧画面にルート内認可を追加し、名簿・記録・保存済みレポートを起動コースで取得。管理者も同じ境界。静的描画/認可5件は修正前失敗を確認してから合格。
- 外部到達度は保存契約を変えず、本人に同コースで割当済みの単元だけを取得。DB統合1件追加（未実行）。
- 関連11ファイル97件と型/差分チェック成功。本番未変更。外部到達度を合成する既存画面と合成しないというE7記載の不一致は別途最新版要件と照合する。

## 2026-09-11 13:13 二重通知防止の候補

- 未適用0014へnotification_claimed_atを追加。送信前の条件付きUPDATEで開始を記録し、1処理だけが通知。開始後はsnapshot上書きも自動再送も行わない。
- 再実行は保存済みを返しreusedで表示を区別。送信結果の記録失敗は成功と報告しない。
- 関連単体100件/型/差分チェック合格。16並行claim等のDB統合3件追加、未実行。未接続や失敗後の確認付き再送手順、保持期限処理とclaim維持の整合は未完。本番未変更。

## 2026-09-11 13:29 保持期限の準備

- redactReportStudentで対象行とその集計への影響を除外し、平均/件数/アラートを再計算。入力不変・他学生保持・全員除外・冪等を含む4単体追加、関連43件合格。
- まだ削除APIへ接続していない。並行生成が退会者情報を再保存/送信する競合を防ぐ設計と一緒に実装する。送信開始記録を消して再送可能に戻さない。本番データ変更なし。

## 2026-09-11 13:52 生成と保持期限処理の接続

- 共通の専用接続ロックへ生成とpurgeを接続。生成の全DB処理に同じ接続を明示し、purgeは同じ接続のtransactionで個別データ削除と新旧週次payloadの除外をまとめる。送信claim/週次行は残す。
- 関連13ファイル86件、型/差分チェック合格。新旧payload除外・rollback・競合拒否のDB統合3件追加（未実行）。本番データ削除なし。
- APIのbusy応答、DB/E2E/全回帰、他の書込み経路と退会後アクセス制御、移行・本番検証が残る。

## 2026-09-11 13:59 API失敗時の扱い

- 週次生成と保持期限APIでbusyは503、他の処理失敗は内部情報を含まない500。保持期限APIは削除成功件数と監査成功件数を区別し、途中失敗後は追加削除を行わない。
- 保持期限API本体の管理者/Origin検証と不正本文拒否を追加。モックによる回帰失敗を確認して修正し、関連37件/型/差分チェック合格。本番削除なし。
- 新しい必須Originに合わせたE2E確認を行う。DB・本番検証は引き続き未完。
