# 手動対応リスト（柴田さま／Cowork側の作業）

コードは実装済みだが、**外部サービス・鍵・実機・書類**が絡むため私（開発）側だけでは
完了できない項目。あとから追えるようにここへ集約する。最終更新: 2026-08-28。

## A. すぐ有効化できるもの（Coworkが `.env` / サーバー操作）

| # | 項目 | 対応 | 関連 |
|---|---|---|---|
| ~~A1~~ ✅完了 | 実AI講師を有効化 | 完了（2026-07-31）。`.env` に `AI_PROVIDER=claude`・`ANTHROPIC_MODEL=claude-haiku-4-5-20251001`・`ANTHROPIC_API_KEY` を設定し再起動。Anthropic実API 200。**画面で本物応答＋電話番号マスキング（未成年フィルタ）を実地確認済み**（CLAUDE.md §9のマージ要件を実LLMで充足）。生キーの一時ファイルはサーバーから削除済み | B. コード実装済み（`src/lib/ai/claudeClient.ts`） |
| A2 | Canvas接続先 | `.env` の `CANVAS_BASE_URL` を実ポートに（`detect-canvas-url.sh`で自動判定済みなら不要） | 完了済みのはず |
| A3 | Canvasリッチデータ投入 | `bash infra/canvas/seed-canvas-rich.sh` を実行（接続先修正後） | 実行待ち |
| A4 | デモデータ表示 | 再デプロイ時に `-e DEMO_RICH_SEED=1` | 済／要確認 |
| A5 | 本番ではCookieロールを付けない | 本番デプロイに `DEV_COOKIE_ROLES` を**設定しない**（LTI運用のため不要）。E2E/デモのみ1 | F. M-2実装済み |

## B. 外部サーバー構築が必要

| # | 項目 | 対応 | 関連 |
|---|---|---|---|
| ~~B1~~ ✅完了 | JupyterHub本体 | さくらの既存サーバーに相乗りで構築（CPU・デモ稼働。2026-07-31 SSO疎通確認）。構築ファイル `infra/jupyter/`。9月にGPUサーバーへ同一構成で移設 | D |
| ~~B2~~ ✅完了 | JupyterHubをCanvasにLTI登録 | 完了（client_id `10000000000003`）。ハマり2点を解決: ①ltiauthenticatorの設定JSONは公開鍵を含まずCanvas必須→`make-canvas-config.sh`で公開鍵入りJSONを「JSONを貼り付け」で登録。②新しい「Canvas Apps（LTIレジストレーション）」方式が有効なため旧「By Client ID」はロックされる→開発者キー一覧の「Canvas Appsで表示する」→**アプリをアンロック**＋**利用可能**にしてからコースのナビで表示 | D |
| B3 | 本番TLS・独自ドメイン | sslip.io暫定 → 独自ドメイン取得＋Let's Encrypt（Caddyのホスト名変更） | F |
| B4 | AGS/NRPSの本番配線 | 成績のツール署名書戻し（AGS）・名簿取得（NRPS）のクライアントは実装済み（`src/lib/lti/services/`）。grade routeを現行のREST書込みからAGSへ切替える配線は、**実LTI起動でAGSクレーム（lineitem/scope）が届く状態**での検証が必要。実Canvasで開発者キーにAGS/NRPSスコープを付与し、起動→クレーム受領を確認してから切替える | C |

## B'. データ永続化・インフラ（ホスティング先確定後＝未決#2）

| # | 項目 | 対応 | 関連 |
|---|---|---|---|
| ~~B5~~ ✅構築完了 | インメモリ→DB | **コード実装済み（2026-08-26）・さくら実サーバー構築完了（2026-08-27）**。PostgreSQL（Drizzle ORM）へ移行。提出・学習記録・デバイス割当・監査ログの全て。ローカルの実PostgreSQLに対し単体テスト161件・E2E258件をPASS確認済み。構築は一括スクリプト（`infra/custom-layer/bootstrap.sh`。冪等）で実施。実サーバーでPostgreSQL 16 healthy・マイグレーション成功・最小シード投入・アプリ200 OKを確認済み。資格情報は台帳へ転記済み。詳細は同ディレクトリのREADME | E |
| ~~B6~~ ✅適用済 | 監査ログの追記専用テーブル | **コード実装済み（2026-08-26）・実サーバーへ適用済（2026-08-27）**。アプリ実行時ロール（aischool_app）にUPDATE/DELETE権限を与えないDB移行（`drizzle/migrations/0001_grant_app_role.sql`）。DBロールでの拒否を単体テストで検証済み（SEC-2） | E |
| ~~B7~~ ✅設定済 | バックアップ | 日次スナップショット＋7日保持（要件定義書8章）。**2026-08-27にcron登録完了**（毎日3:00ダンプ／3:30に7日超を削除。手動実行で疎通確認済み）。リストア演習は未実施→B12。設定内容:<br>`cd ~/AIschool-system/infra/custom-layer && docker compose exec -T db pg_dump -U aischool_admin aischool \| gzip > ~/backups/aischool-$(date +\%F).sql.gz`<br>を日次で登録し、`find ~/backups -name 'aischool-*.sql.gz' -mtime +7 -delete` で7日保持にする | E |

## B''. 2026-08-27 初回構築で判明した要対応（さくら実サーバー）

初回構築は成功（PostgreSQL 16 healthy・マイグレーション成功・最小シード投入・
アプリ200 OK・`ALLOW_DEV_RESET`/`DEV_COOKIE_ROLES`未設定）。以下は構築報告から
判明した積み残し。

**B8〜B12は2026-08-27に対応完了**（`127.0.0.1:3001->3000/tcp`・Next 16.3.3・
LTI設定8変数の反映・リストア演習の件数一致まで実機確認）。
**B13も完了（2026-08-27）**。受講生・管理者の両ロールで実起動を確認済み。残るはB11の後片付け（低優先）と、B14（週次レポートのcron登録）のみ。

| # | 項目 | 対応 | 状態 |
|---|---|---|---|
| ~~B8~~ ✅適用済 | アプリが0.0.0.0で直接公開されていた | `docker-compose.yml` の公開先を `127.0.0.1` に限定するよう修正済み（2026-08-27）。サーバー側で `git pull` → `cd infra/custom-layer && docker compose up -d app` を実行して再適用する。既存のCaddy/nginxが3001等を見ている場合は `APP_HOST_PORT=3001` を付ける。適用後 `docker compose ps` の PORTS が `127.0.0.1:xxxx->3000/tcp` であることを確認 → **2026-08-27 適用完了**（`127.0.0.1:3001->3000/tcp`・外部HTTPS 200を実機確認） | ✅ |
| ~~B9~~ ✅適用済 | 依存パッケージの脆弱性 | high 4件（Next.jsのMiddleware/Proxy bypass含む）を Next 16.2.10→16.3.3 更新で解消済み（2026-08-27。E2E258件で回帰なしを確認）。サーバーで `git pull` → `docker compose up -d --build app` で反映。残る moderate 4件は devDependency の drizzle-kit→esbuild 系（開発サーバー向けの脆弱性で本番実行経路から到達しない）ため据え置き → **2026-08-27 適用完了**（サーバー実機で 16.3.3 稼働確認） | ✅ |
| ~~B10~~ ✅設定済 | LTI環境変数がDockerコンテナに渡っていなかった | 旧コンテナ `aischool-custom` の環境から8変数すべてを回収し `.env` へ反映・コンテナ内で設定済みを確認（2026-08-27）。**ただしCanvasからの実起動によるロール確認は未完**（Canvasタブの再読込がタイムアウト）→ B13 | ✅（起動確認はB13） |
| B11 | 旧デプロイの後片付け | 調査完了（2026-08-27）: systemdサービス・ユニットとも**該当なし**。旧コンテナ `aischool-custom` は停止済みだが restart policy が `unless-stopped`。手動停止済みのため再起動時に自動起動はしない（Dockerの仕様）が、**誤って `docker start` すると現行アプリと3001番で競合**する。`.env` のLTI設定値をパスワード管理台帳へ転記済みであることを確認したうえで `docker update --restart=no aischool-custom` を実行し、不要が確定したら `docker rm` する | **要実施（低優先）** |
| ~~B12~~ ✅完了 | リストア演習 | 要件定義書F6-3・テスト計画書4章の必須項目。2026-08-27実施。別DB `aischool_restore` へ復元し件数が期待値と完全一致（assignments=1／submissions=1／lesson_records=7／device_assignments=16）。照合後に該当DBを削除し残存0を確認。本番DBへのリストアは行っていない | ✅ |
| ~~B13~~ ✅完了 | Canvas実起動でのロール確認（LTI疎通の最終確認） | **2026-08-27 完了**。①受講生側: 管理者のmasqueradeでデモ生徒01としてコースナビ「AI講師・演習」を起動し `きょうやること` を確認。②管理者側: masquerade解除後に同タブを起動し、`せんせい用メニュー`（7項目）＋`管理者メニュー`（監査ログ・Canvas連携状況）の表示を確認。これによりid_token検証・**deployment_idの一致**・`LTI_SESSION_SECRET`・ロール写像（Learner→student／Administrator→admin）・SameSite=None Cookieの往復・`proxy.ts`の権限出し分けが、本番LTI環境で成立していることを実機で確認した。※課題一覧が空なのは正常（実CanvasユーザーIDへの学習データ接続は次段階） | ✅ |
| B14 | **週次レポート生成のcron登録** | 2026-08-28にF4①（週次レポートの自動生成・通知）を実装。サーバーで `git pull` → `docker compose up -d --build app` の後、crontabへ次を追加する:<br>`0 7 * * 1 cd ~/AIschool-system/infra/custom-layer && docker compose exec -T app npx tsx scripts/generate-weekly-report.ts >>~/backups/weekly-report.log 2>&1`<br>登録前に手動実行で疎通確認する（`docker compose exec -T app npx tsx scripts/generate-weekly-report.ts`）。マイグレーション（`weekly_reports` テーブル追加）も必要: `docker compose run --rm --no-deps app npm run db:migrate`。Canvas通知が届くかは講師アカウントの受信箱で確認 | **要実施** |

## C. 書類・規程・判断

| # | 項目 | 対応 |
|---|---|---|
| C1 | 補助金の計上・報告の扱い | 外注費の扱い変更は補助金事務局へ確認（CLAUDE.md 10章の原則）。確認後に10章を更新 |
| C2 | Pマーク運用との突合 | ①保持期間Nの確定（既定3年→確定後 `.env` の `RETENTION_YEARS` を設定）②退会者一覧（studentId・退会日）の供給元＝校務システム/Canvasの連携（現状はAPIへリクエストで渡す想定）③保持期限削除の定期実行（cron等で `/api/admin/retention/purge` を管理者権限で叩く、または本番は削除バッチ化）④監査ログ自体の保持期間（学習データとは別基準）。判定ロジック・削除API・監査記録はコード実装済み（`src/lib/retention/policy.ts`・`/api/admin/retention/purge`） |
| C3 | 未決事項の確定 | `docs/未決事項.md` の #3/#4/#7/#8/#10/#11 |

## D. 実機（9月・機材搬入後）

| # | 項目 |
|---|---|
| D1 | 3条件（NUC＋モバイルモニター/Quest3/NearHub）表示確認（2026-08-24: GOOVIS廃止） |
| D2 | 16クライアント同時操作の負荷テスト |
| D3 | 校内GPUサーバー（NVIDIA L40S 48GB。旧RTX 6000 Ada）でのローカルLLM・CloudXR（JupyterHubは現行カリキュラムでは不使用） |
| D4 | 低帯域・90分連続の配信試験（VR連携時） |

---

（実装が進むたびに、有効化に必要な操作を本リストへ追記する）
