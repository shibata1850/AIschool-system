# ホスティング方針（段階移行）

- 決定: 2026-07-04 柴田さま（docs/未決事項.md #2）
- 方針: カスタム層はまず**Vercel**でデモ・レビュー運用し、本格運用の段階で
  **さくらのクラウド**へ移管する。Canvas LMS本体は最初からさくらのクラウドに置く

## 1. 何をどこに置くか

| コンポーネント | デモ期（〜8月デモ） | 本番期（9月〜開校） |
|---|---|---|
| カスタム層（本リポジトリ・Next.js） | Vercel | さくらのクラウド（Docker） |
| Canvas LMS本体（Rails+PostgreSQL+Redis） | さくらのクラウド | さくらのクラウド |
| AI推論・JupyterHub・CloudXR | 校内GPUサーバー（オンプレ） | 校内GPUサーバー（オンプレ） |

**重要: Canvas LMS本体はVercelには載りません。** VercelはNext.js等の
フロントエンド／サーバーレス向けで、Railsの常駐サーバー＋PostgreSQL＋Redisと
いう構成は対象外です。Vercelを使う場合も、Canvasを動かす時点でさくらの
クラウド（または他のサーバー）が別途必要になります。

## 2. Vercel→さくら移管の互換性評価

### 問題にならない点（移管前提で実装済み）

- Vercel固有機能（KV / Blob / Cron / Edge Config等）は**一切使っていない**。
  標準の `next build` / `next start` で動く構成
- 本番ビルド（`next start`）に対してE2E 154件のPASSを継続確認しており、
  これはさくらでの動作形態そのもの
- 権限ガード（proxy.ts）はCookie読み取りのみでEdge互換
- 環境変数は `.env.example` に集約済み。Vercelのダッシュボードでも
  さくらのDocker環境でも同じ変数名で設定するだけ

### 注意が必要な点（Vercel利用時の制約）

1. **インメモリストアはサーバーレスで保持されない（最重要）**
   参照実装はデモデータをメモリに持つ。Vercelはリクエストごとに別インスタンスへ
   割り振られ得るため、「提出→採点→講師確認」の一連の操作デモは
   **データが消える／別インスタンスで見えない**ことがある。
   - Vercelデモの用途は**画面デザイン・文言のレビュー中心**と割り切る
   - 操作フローの通しデモが必要になったら、その時点でさくらに
     Docker 1台を立てるのが早い（それがそのままステージングになる）
2. **バックグラウンドAI採点の打ち切り**
   サーバーレスは応答返却後の処理を保証しない。Vercelで実AI採点まで動かす場合は
   Next.jsの `after()` への載せ替えが必要（さくらの常駐サーバーでは不要）
3. **実行時間の上限**
   Vercelの無料プランは関数の実行時間が短く、実AI応答（数十秒）が
   打ち切られる可能性がある。デモはモックAI（AI_PROVIDER=mock）のままを推奨
4. **個人情報の取り扱い**
   Vercelには実受講生の個人情報を載せない（架空デモデータのみ）。
   国内保管方針・Pマーク整合（要件定義書5.3）は、さくら移行後に満たす

### 「環境変数が必要になったら移行」について

環境変数の設定自体はVercelでも問題なくできる（ダッシュボードで設定）。
実際の移行トリガーは環境変数ではなく、次のいずれかが正確:

- Canvas本体を立てる時（どのみちさくらが必要になる）
- 提出データ等の**永続化**が必要になる時（インメモリ→DB）
- 実受講生データを扱い始める時（国内保管・Pマーク整合）

## 3. 移管手順（さくらのクラウドへ）

1. さくらのクラウドにサーバー確保（Ubuntu + Docker）
2. 本リポジトリの `Dockerfile` でイメージをビルドして起動:
   ```
   docker build -t lms-custom .
   docker run -d -p 3000:3000 --env-file .env lms-custom
   ```
3. Vercelに設定していた環境変数を `.env` へ移す（値はパスワード管理台帳から）
4. リバースプロキシ（nginx等）でTLS終端し、ドメインのDNSを切替
5. E2Eをさくら環境に対して1回実行して受け入れ確認

コードの変更は不要（同じ `next build` 成果物が動く）。

## 4. SOFTDOINGマネージド・ホスティング（方向性確定 2026-08-06）

- 決定: 2026-08-06 柴田さま。**さくらのクラウドの契約・アカウントはSOFTDOINGが保有し、
  その上で運用する**。対象は次の2つ:
  1. AIスクール／クラウドキャンパス本体（Canvas＋カスタム層）
  2. **卒業企業が開発した本番業務システムの運用**（＝SOFTDOINGがマネージド・
     ホスティングとして請け負う。企業ごとの自社契約ではなくSOFTDOINGのさくらに載せる）

### 設計原則

- **テナント分離**: 1社＝専用サーバー/セグメントで分ける。複数社を共用サーバーに
  相乗りさせない（下記「なぜ相乗りさせないか」）
- **可搬性（囲い込まない）**: OSS＋標準Docker構成のため、企業が独立を望めば
  他社クラウド・自社アカウントへそのまま持ち出せる。これを明示して信頼を得る

#### なぜ相乗りさせないか（テナント分離の根拠）

本事業は「止めない・守る」を約束して対価を得るマネージド・ホスティング。相乗りは
その約束を構造的に破るため、既定は1社1サーバー分離とする。

1. **情報漏洩・データ混在**: 複数社の業務データ（要配慮の個人情報を含む）を同一
   サーバー/DBに置くと、設定ミスや不具合で他社データが露出しうる。分離すれば事故が
   1社内に留まる。SOFTDOINGはPマーク運用の責任も負う（本章末の未決論点も参照）
2. **障害の巻き込み（ノイジーネイバー）**: 1社の暴走・資源枯渇・アクセス集中が
   同居全社を巻き込む。分離＝「1社の問題は1社だけ」で他社の稼働を守れる（SLAの土台）
3. **セキュリティ侵害の封じ込め**: 1社のアプリ侵害から隣社へ横移動されるのを防ぐ
4. **メンテ・再起動の独立性**: 更新・再起動・バックアップを1社の都合だけで実施できる
5. **責任分界・課金・持ち出しの明確さ**: 「この顧客＝このサーバー＝この原価/責任」と
   切り分けられ、請求が透明。可搬性（上記）も分離が前提で成り立つ

トレードオフ: 相乗りは台数が減りコストは下がるが、上記リスクは業務・個人データを
預かる事業では節約額に見合わない。1社分の追加原価（月¥1.5万〜4万）を請求できるため
分離でも採算は取れる。**例外**: 社内数人のみ・個人情報を扱わない・停止しても軽微、
といった低リスクの小規模システムに限り共用の廉価枠を設ける余地はある（線引きを
明文化したうえで。既定はあくまで分離）。
- **課金**: さくらへの支払いはSOFTDOING。企業へは「さくら原価＋運用・監視・
  サポートの人件費」を月額サービスとして請求（人材＋ツール＋運用バンドルの継続収益化）

### 費用の目安（計画用概算・さくら標準プラン逆算）

| ケース | 構成 | さくら原価/月 |
|---|---|---|
| スクール本体（開校時・16名同時＋実データ） | 4Core-16GB＋SSD250GB＋バックアップ | 約¥2.7万〜2.9万 |
| 卒業企業の複雑めシステム（1社・1台構成） | 同上 | 約¥2.5万〜4万 |
| 同（アプリ＋DBを2台に分離） | ＋DB用VM | 約¥3.5万〜4万 |

- 共有セグメントは**下り転送量の従量課金なし**＝動画配信・アクセス増でも原価は据え置き
  （100Mbpsは費用でなく速度の上限。大規模配信時は帯域増速 or オブジェクトストレージ/CDN）
- 企業請求額 = 上記原価 ＋ 運用マージン（サービス価格は別途設計）

### 今後詰める論点（未決）

- 企業請求の価格設計（原価＋運用費の月額メニュー化）
- SLA・責任分界（稼働保証、障害対応時間、バックアップ世代・復旧目標）
- 複数テナントの標準運用手順（監視・日次バックアップ・セキュリティ更新の共通化）
- **Pマーク範囲の拡大**: 顧客企業の個人データをSOFTDOING管理下で扱うため、
  個人情報の取扱い範囲・委託契約・保持/削除手順を要件定義書5.3と整合させる
