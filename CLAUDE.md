# CLAUDE.md — Next Gen AI School LMSカスタム層

このファイルはClaude Codeがこのリポジトリで作業する際の規約である。
すべての実装・修正・レビューはこの規約に従う。

---

## 1. プロジェクト概要

- 事業者: SOFTDOING株式会社
- 対象: Next Gen AI School（岩手県北上市・北上駅前、2026年10月開校）
- **本校のゴール（2026-08-24 パンフレットv2で確定）**: 技術習得そのものではなく、
  受講生が自社の業務課題を**40時間（1日4時間×10日・通学制）でBASE44により
  本番稼働システムとして1本完成させる**こと（「研修を受けさせるのではない。
  作らせて、帰す。」）。知識がなくても作りやすいノーコード寄りのBASE44を
  採用しているのはこのため
- **BASE44自体（実際のシステム実装環境）は本リポジトリの範囲外**（別会社の
  外部SaaS。受講生がブラウザで直接操作する）。本リポジトリは、その学習を
  支える**Canvas LMSカスタム層**（教材配信・演習記録・到達度可視化・
  セキュリティ）を担う
- 本リポジトリの範囲: Canvas LMS OSS版を基盤とした**カスタム層**の開発
  - LTI 1.3 外部ツール群（プロンプト演習管理。AI講師の用途は未決 — 12章・未決事項#3）
  - Canvas REST API 連携（学習ログ収集、到達度ダッシュボード）
  - JupyterHub 連携（技術演習の参考実装。現行カリキュラムでは不使用 — 12章）
- 本リポジトリの範囲外: Canvas LMS 本体のソースコード、BASE44実装環境そのもの
- 詳細仕様: `docs/要件定義書.md` を必ず参照してから実装に着手する

## 2. 絶対ルール（違反禁止）

1. **Canvas LMS 本体をフォーク・改変しない。** カスタマイズは LTI 1.3 外部ツールと REST API / GraphQL 経由のみ。本体改変が必要に見えた場合は実装せず、代替設計を提案して人間の判断を仰ぐ
2. **E2Eテストなしのコミット禁止。** 詳細は「7. テスト規律」
3. **受講生の実個人情報（氏名・連絡先・学習ログ）をコード・fixtures・コミットに含めない。** テストデータは架空値のみ
4. **シークレット（APIキー、DB接続情報）をコミットしない。** `.env` は gitignore 済みであることを毎回確認
5. **UIテキストはすべて日本語。** 対象ユーザーは企業・行政の社員（社会人）。専門用語には振り仮名または平易な言い換えを添える（2026-08-24: パンフレットv2で対象が企業・行政向け人材育成プログラムへ確定。旧「小中高生〜シニア」は誤り。UIの読みやすさ原則自体は維持する）
6. 破壊的操作（DB削除、本番デプロイ、force push）は実行前に必ず人間へ確認する

## 3. システム構成（3層）

```
[クラウド層] Canvas LMS OSS（Docker: Rails + PostgreSQL + Redis）
     │        ホスティング先: さくらのクラウド or AWS（未決 → docs/未決事項 #2）
     │ HTTPS / LTI 1.3 / REST API
[サーバー層] HP Z4 G5 + NVIDIA L40S 48GB（教室内オンプレGPU共有機。講師機兼用・収納内設置）
     │        AI講師推論（用途は未決 — 12章）・JupyterHub（技術演習の参考実装）
     │ 校内LAN: Aruba AP-655 / 10GbE L2スイッチ / APC Smart-UPS
[端末層]     下記デバイス構成（4章）。実装作業そのものはBASE44（ブラウザ・外部SaaS）
```

## 4. 表示アーキテクチャ（教室デバイス構成）— UI実装の前提

**2026-08-24 パンフレットv2で構成が刷新された。GOOVIS（HMD据え置き画面）は
デバイス構成から外れ、NUC＋モバイルモニターが生徒の主画面（実装作業用）になった。**
40.44㎡のL字型教室、西ゾーン8席＋東ゾーン8席（うち1席講師席）＝全16席。

| デバイス | 台数 | 用途 |
|---|---|---|
| ASUS NUC 13 Pro + 15.6インチモバイルモニター | 各16 | **生徒の主画面**。BASE44実装工程（STEP05〜09）はこれで作業する |
| Meta Quest 3 512GB | 16 | 西ゾーン配置・2人1組運用。VR/MRで**自社の業務を捉え直す**工程（STEP01〜04・10）に使用。旧構想の「AI講師との没入授業」用途ではない |
| LED（西4,250mm／東5,700mm） | 各1 | 連結して1映像、分離して別々の内容の両方に対応。1社2班運用・2社同時受け入れにも使う |
| NearHub S65 + Aladdin X2 Light | 各1 | 講師機（西壁設置）からの教室投影・板書 |
| ASKA3D-Plate | 1 | 裸眼・空中ディスプレイ（共有デモ用） |
| iPad(A16) 11インチ | 10 | 教材・手元表示用 |
| Cleanbox CX-1 | 1 | VR機器のUV除菌ボックス（返却時に除菌） |

### UI設計制約（全画面共通・必読）

- **生徒の実装画面（NUC＋15.6インチモニター）**: 一般的なノートPC相当の解像度を想定。本文フォント 16px 以上、ダークテーマ標準、1画面1タスクは維持（旧GOOVIS越し前提の記述はそのまま流用できる基準として残す）
- **Quest 3 表示**: 業務観察・可視化コンテンツを想定した高コントラスト表示。細線・小アイコン禁止
- **講師ダッシュボード**: NearHub S65 のタッチ操作前提。タップターゲット 44px 以上、右クリック依存禁止
- 新規画面は「NUC＋モバイルモニター / Quest 3 / NearHub」の3条件での表示確認をタスク完了条件に含める（GOOVIS条件は廃止）

## 5. 技術スタック

- Canvas LMS: OSS版最新安定リリース、Docker Compose 構成
- カスタム層: TypeScript / Next.js（LTI 1.3 ツール、ダッシュボード）
- カスタム層のデータ永続化: PostgreSQL（Drizzle ORM）。Canvas本体のDBとは別コンテナで分離する（絶対ルール1参照）。実行時アプリ用ロールと、マイグレーション・E2Eリセット専用の管理ロールを分離し、監査ログはDBロールでUPDATE/DELETEを拒否する（追記専用の強制。9章）。詳細は `infra/custom-layer/README.md`
- AI推論: 校内GPUサーバー上のローカルLLM か Claude API か未決（未決事項 #3）。**推論クライアントは必ず抽象化レイヤー（interface）を挟み、後から切替可能に実装する**。AI講師機能自体の位置づけは12章参照
- 演習環境: JupyterHub（技術演習の参考実装。現行カリキュラムでは不使用 — 12章）
- 実装環境（本リポジトリ外）: BASE44（受講生が業務システムを組む本体）
- E2E: Playwright

## 6. 開発環境

- 開発機: HP Z4 G5 Workstation（Windows 11）。講師機・コンテンツ制作機と兼用のため、**開発用ユーザーアカウントを分離し、授業時間帯のビルド・負荷処理を避ける**
- Claude Code: Windows ネイティブ版。Git for Windows 導入済み（Bash ツール有効）
- ターミナル: cmd 標準（PowerShell も可）。ドキュメント内のコマンド例は cmd 表記で書く
- リポジトリ: GitHub（SOFTDOING organization）

## 7. テスト規律（Definition of Done）

SOFTDOING 社の E2E 義務化ルールを本リポジトリにも適用する。

機能が「完了」となる条件（すべて必須）:

- [ ] 機能コードを書いた
- [ ] 該当機能の Playwright E2E テストを書いた（正常系・入力エラー系・権限系・境界値の4パス）
- [ ] E2E がローカルで PASS した
- [ ] 3条件（NUC＋モバイルモニター解像度 / Quest 3 想定 / NearHub タッチ）で表示確認した
- [ ] 既存テストが全て通る（リグレッションなし）
- [ ] テストコードを本体コードと同じコミットに含めた

追加ルール:

- バグ修正は「再現E2Eを先に書き、FAILを確認してから直す」。修正後のテストは `e2e/regression/` に日付付きで残す
- `waitForTimeout` の固定待機禁止。`expect(...).toBeVisible()` の自動待機を使う
- セレクタ優先順: `getByRole` > `getByLabel` > `getByText` > `getByTestId`。CSSセレクタは最終手段
- `.skip` `.only` を残したコミット禁止
- 負荷要件: 演習系機能は **16クライアント同時操作** を模した負荷テストを受け入れ前に1回実施

## 8. コーディング規約

- コミットは Conventional Commits 準拠（例: `feat: プロンプト演習の提出機能を追加（E2Eテスト付き）`）
- 機能コードと E2E は同一コミット。E2E だけ後付けするコミット禁止
- 到達度スコア等の計算ロジックには必ず単体テストも付ける（E2E の代替にはならない、両方書く）
- ログ出力に個人情報を含めない（ユーザーIDのみ可、氏名不可）

## 9. セキュリティ・個人情報

- 受講生は企業・行政の社員（社会人）。学習ログ・提出物は要配慮の個人データとして扱う（2026-08-24: 対象確定により「未成年が含まれる」という前提は誤り。要配慮データとしての扱いは維持する）
- 監査ログ: データの作成・更新・削除はすべて記録（操作者・日時・変更前後）。閲覧は管理者ロールのみ
- AI講師の出力にはコンテンツフィルタを必ず通す。フィルタ実装前に AI 応答を生徒へ直接表示する機能をマージしない（既存の未成年向けフィルタ実装はそのまま安全側の基準として使ってよい）
- SOFTDOING 社の Pマーク運用と整合させる。保持期間・削除手順は要件定義書 5.3 に従う

## 10. 役割分担（2026-07-03 方針決定 — 詳細は docs/未決事項.md 確定済み#1）

- 有限会社ノーティ担当分: 見積 No.13020653 の6項目（正式な開発・納品主体。事業報告の対象）
- SOFTDOING 担当分: **本リポジトリでの参照実装（バックアップ）の並行開発**。過去の委託案件での要件未達リスクに備えた内部成果物であり、ノーティの納品物とは別リポジトリ・別成果物として管理する
- **補助金の証憑（外注費）と実装実態を一致させること。本リポジトリ（SOFTDOING 側）で実装した成果物をノーティ委託分として計上・報告しない**
- 参照実装を本番採用する事態になった場合は、採用前に補助金事務局へ報告整合（外注費の扱いの変更等）を確認する。無確認での差し替え禁止

## 11. やらないこと（実装禁止・スコープ外）

- Canvas LMS 本体の改変・フォーク
- **BASE44実装環境そのものの開発・保守**（外部SaaS。受講生が直接操作する）
- **通信制・録画配信によるeラーニング受講**（本コースは通学制のみ。2026-08-24 パンフレットv2で確定 — eL関連の既存コード・テストは削除せず参考実装として残すが、新規のeL専用機能は追加しない）。**2026-08-31: eラーニングは別発注・別補助金の姉妹システムとして実施されることが確定したため、この禁止は「二重計上の回避」という更に強い理由を持つ。具体的な禁止範囲は13.1**
- 決済・請求機能（フェーズ1対象外）
- 保護者ポータル（フェーズ2候補、要望リストへ）
- 多言語対応（日本語のみ）
- 独自ビデオ会議機能（既存ツールを利用）
- VR コンテンツ制作パイプライン（本リポジトリは LMS 層のみ）
- 成績証明書の公式発行機能

スコープ外の要望が出た場合は実装せず、`docs/要望リスト.md` に記録して次フェーズ判断に回す。
**ただし 13.1 に該当する要望は要望リストにも回さず、eラーニング側の範囲として差し戻す。**

## 12. パンフレットv2（2026-08-24）による構想転換 — 経緯と現状

事業の実態がパンフレット改訂（`NGAS_A4_5P_v2`）で大きく具体化された。本章は
その差分と、本リポジトリで判断・保留した事項を記録する。詳細は
`docs/要件定義書.md` 冒頭の変更履歴、`docs/要望リスト.md` #7・#8 も参照。

**確定した転換**:
- ゴールは技術習得ではなく、**受講生ごとに1本、本番稼働する業務システムを
  BASE44で完成させること**（1章参照）
- 受講形態は**通学制のみ**。eラーニング（自宅受講）は行わない
- デバイス構成からGOOVISが外れ、NUC＋モバイルモニターが生徒の主画面（4章参照）
- Quest 3/VRの用途が「AI講師との没入授業」から「**自社の業務をVRで捉え直す**」
  （業務観察・可視化）へ変わった
- GPUサーバーは NVIDIA L40S 48GB（旧: RTX 6000 Ada 48GB）
- 所在地は北上駅**前**（大通り1丁目3番1号。旧: 北上駅西口という表記だった）

**保留・要確認のまま進めている事項**（実装済み機能を独断で削除しないため）:
- **JupyterHub**: パンフレットのカリキュラムに登場しない（実装工程は全てBASE44）。
  「参考実装として保留、現行カリキュラムでは不使用」の扱いとし、コード・
  インフラ（`infra/jupyter/`）は削除せず残す
- **AI講師（Claude APIによるQ&Aチャット）**: パンフレットに明示の記載はないが、
  2026-08-24 柴田さまより「動画演習（要望リスト#6）で使用予定」と判明。
  ~~詳細な用途は要望#6の設計着手時に確定~~
  → **【2026-08-31 この位置づけは無効】** 要望#6（動画演習）はeラーニングシステム側の
  範囲となり本リポジトリでは実装しないため（13.1）、AI講師の用途は未決に戻った
  （未決事項#3）。**機能自体は残す** — 先方文書2.2が「AI講師との対話機能＝
  クラウドキャンパスのF2」と外部から明記したため（13.2）
- **要件定義書のKPI#3（eラーニング自走率）**: eL廃止により無効。
  → **【2026-08-31 廃止で確定】** eラーニングは別システムとして実施され、同趣旨のKPIは
  先方のKPI#1・受け入れ基準B-8が担う。代替KPIは設けない（未決事項#12）

**新たに欲しいと分かった機能**（未実装・要望リストに記録済み）:
- 事前ヒアリング機能（開発テーマ決定を支援するオンライン聴取フォーム/フロー）
- 複数人ディスカッションのメモ取り→要件定義書の自動生成機能

## 13. 姉妹システム「eラーニングシステム」との境界（2026-08-21要件定義書 第1版）

**本リポジトリの対外呼称は「クラウドキャンパス」**（発注書 SD-2026-0720-001／
新事業進出補助金枠）。これとは**別に**、ノーティが「eラーニングシステム」
（発注書 SD-2026-0821-004／見積 No.13020711 ¥609,800／**岩手県中小企業賃上げ補助金**）
を開発する。別リポジトリ・別インスタンス・**別の補助金で計上する別システム**。

**なぜ規約に書くか**: 先方の要件定義書 第2章が最重要事項として
「**両者の機能が重なると、補助金の実績報告で二重計上と判断される**」と明記している。
2章の絶対ルールと同格で扱う。

### 13.1 本リポジトリで実装してはならない機能（eラーニング側の範囲）

以下は**要望が来ても実装しない**。`docs/要望リスト.md` にも回さず、
「eラーニングシステム側の範囲」として差し戻す:

- 動画教材の視聴管理（再生位置・視聴進捗・視聴完了判定）
- 章末小テストの出題・自動採点・誤答蓄積
- AI習熟度分析（単元別到達度の推定・弱点抽出・復習問題の出題）
- 受講生本人／企業受講担当者への受講レポート配信
- 教材コンテンツ管理（動画・設問の登録、CSV一括取込）
- 自宅・職場からの非同期学習を成立させるための機能一般

> **要望リスト #6（動画24時間視聴＋視聴回数管理＋視聴後小テスト＋成績連動アドバイス）は
> 丸ごとこの禁止範囲に該当する。本リポジトリでは実装しない**（→ 要望リスト #6 参照）。
> これに伴い、AI講師（F2）の「動画演習で使う」という位置づけ（12章）も無効になった。

### 13.2 逆に、本リポジトリの範囲だと先方文書が明記したもの

eラーニング側の禁止事項（先方2.2）として列挙された ＝ **クラウドキャンパスの機能**。
これらは削除・縮小しない:

- コース・クラス・時間割の管理（**マスタの「正」は本リポジトリ側**）
- AI講師との対話機能（F2）
- Jupyter Notebook 演習の実行環境（→ 12章の「保留」扱いを見直す根拠になる）
- プロンプト演習の出題・AI一次採点（F3）
- 授業中のリアルタイム進捗モニタリング（F4）
- VR／MR 関連の表示・配信

### 13.3 本リポジトリに**増える**作業（E7連携の受け側）

先方 E7「クラウドキャンパス連携」は、本リポジトリ側の対応がないと成立しない。
未着手 — 詳細と論点は `docs/eラーニング連携.md`、期限は `docs/未決事項.md` #13。

1. **SSO提供**: eラーニングを LTI 1.3 ツール（または OAuth 2.0 クライアント）として
   Canvasの開発者キーに追加する。**本リポジトリのコード変更は原則不要**（Canvas設定作業）
2. **単元マスタの参照API**: 「正」は本リポジトリ側。参照手段の提供が要る
3. **到達度スコアの受信API（新規実装）**: 自宅学習の到達度を受け取り、
   F4ダッシュボードに表示する。先方の受け入れ基準 B-7 に直結する
4. **削除の伝播**: 受講生を本リポジトリ側で削除したら、eラーニング側も無効化される必要がある

**注意**: 3 は「eラーニングの機能」ではなく「クラウドキャンパスの到達度ダッシュボードの
入力を1つ増やす」実装。13.1 の禁止範囲には当たらない（境界の説明を残すこと）。

### 13.4 eラーニング向けの**文書**を本リポジトリに置くことについて

`docs/eラーニング要件定義_補強.md`（背景・目的）と
`docs/eラーニング開発_引き継ぎ.md`（開発担当者への引き継ぎ）は、
**別システム向けに発注者の立場で作成した文書**であり、本リポジトリの実装対象ではない。版管理の都合で置いているだけで、
別リポジトリが立ち上がれば移設してよい。

**文書を書くことと機能を実装することは別である。** 13.1 が禁じるのは実装であって、
発注者として要件を定義することではない。ただし、この文書を根拠に本リポジトリへ
eラーニングの機能を実装してはならない（13.1 は変わらず有効）。
