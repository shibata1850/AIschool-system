# 実装タスク一覧（全機能・本格開発）

- 位置づけ: Next Gen AI School LMSカスタム層の全機能実装のタスク台帳
- 方針: 本格開発として全機能を実装する（デモ用の骨格から本実装へ）
- 記法: [x] 完了 / [ ] 未着手 / [~] 一部実装。〔実機〕は9月の機材搬入待ちの検証を含む
- 最終更新: 2026-08-28（F4① 週次レポートの自動生成を実装。J章追加）

> **2026-08-06 デモ疎通マイルストーン達成（実機・端から端まで確認）**:
> Canvas（架空16名・課題4件・成績投入）→ LTI 1.3 SSO → カスタム層（AI講師=実AI／
> プロンプト演習／到達度／講師画面=**Canvas実データ連携**の名簿・成績入力・成績サマリ）
> → JupyterHub（生徒ごと個別ノート）まで、実際の画面で一通り動作を確認。
> 残る仕上げ（任意）: 提出データの投入、成績のばらつき調整、Canvas読取トークンの権限最小化確認。

---

## A. 基盤（完了済み・現状の到達点）

- [x] 要件定義書・画面仕様書・テスト計画書
- [x] F1〜F4の参照実装（提出・AI採点・状態遷移・到達度・監査ログ）
- [x] Canvas REST APIクライアント（全ページ取得・名簿・レート制限再試行）
- [x] Canvas連携 B-1（名簿・課題読取）/ B-3（成績書戻し）/ B-4（成績サマリ）
- [x] LTI 1.3 L1〜L4（ログイン開始・id_token検証・セッション・実機起動成功）
- [x] UIデザインシステム・共通シェル・ホーム/講師/管理者メニュー・戻る導線
- [x] さくらのクラウド構築・暫定HTTPS（sslip.io＋Caddy）
- [x] デモデータ（DEMO_RICH_SEED＝授業中の教室16席／Canvasリッチ投入スクリプト）
- [x] コード監査対応（オープンリダイレクト・監査操作者・アルゴリズム固定 等）
- [x] 品質: 単体178件 / E2E276件（開発・本番ビルド両方）。Next.js 16.3.3（2026-08-27にhigh脆弱性4件を解消。回帰なし）

---

## B. AI基盤（本物のAI講師）

- [x] `AI_PROVIDER=claude` 実運用化（ANTHROPIC_API_KEY設定・Anthropic実API 200確認。2026-07-31。モデル=claude-haiku-4-5）
- [~] コンテンツフィルタの精緻化（カテゴリ分類＋正規化＋誤検知ガード実装。本番は分類器へ。
  2026-08-24: 対象は企業・行政の社員に確定したため「未成年向け」という前提は誤りだが、
  フィルタ自体は安全側の基準として維持する — CLAUDE.md 9章）
- [~] サーバー側キャンセル実装（AbortSignalを route→tutor→AIクライアントへ配線。中断でAPI/GPUの無駄打ちを止める）＋チャットに「やめる」ボタン。※逐次トークンのストリーミング表示はコンテンツフィルタ（出力全体の検査が必須）を回避してしまうため意図的に不採用。実LLM時は完了応答をフィルタ後にまとめて返す
- [ ] ローカルLLM対応（校内GPUサーバー。未決#3の確定後）〔実機〕
- [ ] プロンプト演習のAI採点を実AIに接続（観点別採点の精度検証）

## C. Canvas連携（書き込み・双方向）

- [~] B-2: 生徒の提出をCanvasへ「提出済み・未採点」として通知（LTI Advantage AGS）。
  起動→セッション→提出APIまで配線済み（2026-08-24。単体テスト9件追加）。
  **未検証**: 実際にはコースナビ起動にはlineitemクレームが付かないため、この配線が
  効くのは課題（アサインメント）としてLTI起動された場合のみ（Deep Linking等の
  設計が別途必要）。lineitem無しの起動では意図どおり静かにスキップする（テスト済み）。
  実機での動作確認は外部依存→手動対応リスト B4
- [~] AGS: ツール署名で成績書戻しするクライアント実装（token/ags）＋起動時エンドポイント取得。grade routeへの配線は実LTI起動でのAGSクレーム検証が要（外部依存→手動対応リスト B4）
- [~] NRPS: 名簿取得クライアント実装（nrps）＋起動時URL取得。画面への配線は残（外部依存→手動対応リスト B4）
- [~] 出席管理をカスタム層で実装（未決#11・Roll Call不採用）。講師が出席/欠席を記録→到達度の出席率へ反映（app/teacher/attendance）。Canvasへの書戻しは方針未確定で保留
- [x] ストアのCanvas/DB永続化（`src/lib/f3/store.ts` のインメモリ脱却）→ E章で詳細

## D. Jupyter演習（S4・SSO疎通まで完了）

- [x] さくらにJupyterHub構築（CPU相乗りでデモ稼働）: infra/jupyter/一式。16名同時・GPU本番は9月に同一構成で移設
- [x] LTI 1.3 SSO（jupyterhub-ltiauthenticator）: Canvas→JupyterHub 再ログインなし起動を実機確認（2026-07-31）
- [x] 受講生ごとの個別ノートブック環境の起動（DockerSpawnerで /user/<id> の個別コンテナ起動を確認）
- [x] カーネル再起動UI（JupyterLab本体機能・Kernelメニューで提供）
- [~] GPUサーバー停止時の静的教材モードへの導線（JUPYTER_FALLBACK_URL・画面実装済み）
- [ ] 16名同時起動の負荷確認〔実機〕
- [ ] （任意）カスタム層 /jupyter の「演習をはじめる」導線（JUPYTERHUB_URL設定。Canvasコースナビから直接入る運用なら不要）

## E. データ永続化・整合性

- [x] インメモリストア → DB（2026-08-26）。PostgreSQL＋Drizzle ORM。提出・課題・学習記録・
  デバイス割当を移行（`src/lib/f3/store.ts`・`src/lib/db/`）。実行時ロール（aischool_app）と
  マイグレーション用ロール（aischool_admin）を分離（最小権限）。単体161件・E2E258件で
  実PostgreSQLに対しPASS確認済み。さくらでの実コンテナ構築は→手動対応リスト B5
- [x] 監査ログを追記専用DBテーブルへ（2026-08-26）。アプリロール（aischool_app）に
  UPDATE/DELETE権限を与えない（`drizzle/migrations/0001_grant_app_role.sql`）。
  DBレベルでの拒否を単体テストで検証（SEC-2。`src/lib/audit/__tests__/appendOnly.test.ts`）→手動対応リスト B6
- [x] 二重提出の楽観ロック（版数チェック。既知残課題#1）。2026-08-26のDB移行で、
  プロセス内awaitの不可分性に依存する実装（複数プロセスでは効かない）から、
  DB側の条件付きUPDATE（`WHERE id=? AND version=?`）へ置き換えた。真の並行実行での
  排他を単体テストで確認（`src/lib/f3/__tests__/optimisticLock.test.ts`）。
  提出（submit）・採点（review）・AI採点（gradingTask）の3経路すべてに適用
- [x] バックアップ（日次スナップショット＋7日保持。要件定義書8章）。2026-08-27にさくら実サーバーでcron登録＋リストア演習（別DBへ復元し件数一致）まで完了→手動対応リスト B7/B12

## F. セキュリティ・本番化

- [x] M-2: 本番でCookieロールを無効化（DEV_COOKIE_ROLES未設定時は昇格させない）。
  2026-08-27にfail-closed化: LTI未設定のまま本番稼働した場合の既定を student から
  **guest** へ変更（旧実装は匿名アクセスが演習・AI講師へ到達しうるfail-openだった）
- [x] M-3: deployment_id を必須化（欠落を拒否）
- [ ] TLS・独自ドメイン（sslip.io暫定 → 本番ドメイン。Canvas/カスタム層とも）
- [~] 氏名マスキングの強化（姓＋敬称の辞書マスキング実装。NERは本番）
- [~] Pマーク整合・保持期限削除（未決#10。在籍中＋退会後N年）。退会者の学習データ（提出・学習記録）を保持期限判定で削除し監査記録する管理者API（/api/admin/retention/purge）＋ポリシー（既定3年・RETENTION_YEARSで上書き）を実装。実運用の退会者連携・定期実行スケジューラ・監査ログ自体の保持期間は残（手動対応リスト F参照）
- [x] 監査ログ閲覧の権限・エクスポート（CSV出力・出力操作も監査記録）

## G. UI/UX・3デバイス（2026-08-24 パンフレットv2でGOOVIS廃止・4条件→3条件）

- [ ] 各画面の磨き込み（S1〜S10。目視レビューのフィードバック反映）
- [ ] NUC＋モバイルモニター / Quest 3 / NearHub の3条件 実機表示確認〔実機〕
- [ ] Quest 3向け表示（文字1.25倍・定型ボタン優先。CloudXR圧縮耐性）〔実機〕
- [x] モバイルモニターの役割確定（未決#4）→ **確定済み（2026-08-24）**: GOOVIS自体が
  構成から外れ、モバイルモニターがNUCとともに生徒の主画面に。コード側も追随済み
  （2026-08-26: `DeviceAssignment.goovisId`→`monitorId`に改名、S9画面の表記修正）

## H. 受け入れ・負荷・テスト

- [ ] 16クライアント同時操作の負荷テスト（演習系・受け入れ前に1回）
- [ ] 低帯域・90分連続の試験（VR連携時）〔実機〕
- [ ] E2Eの拡充（実Canvas接続時のステージングE2E・回帰追加）
- [ ] 4パス（正常・入力エラー・権限・境界値）の全機能網羅チェック

## I. 未決事項の確定（docs/未決事項.md）

- [~] #3 AI推論基盤（Claude API / ローカルLLM）。実装・稼働確認済み（Claude API）。
  位置づけは大枠判明（動画演習・要望#6向け）、詳細用途は要望#6着手時に確定
- [x] #4 モバイルモニターの役割 → **確定（2026-08-24 パンフレットv2）**
- [ ] #7 機材搬入日（結合テスト開始日）〔実機〕
- [x] #8 eラーニング同時接続想定数 → **不要（対象外・2026-08-24）**: 通学制のみに確定しeラーニング自体が無くなった
- [ ] #10 ログ保持期間（Pマーク整合）
- [ ] #11 出席データの管理方式
- [ ] #12（2026-08-24追加）KPI#3の代替指標（eラーニング自走率KPI無効化に伴う）

## J. F4 週次レポート自動化（2026-08-28 完了）

- [x] 週次レポートの自動生成（要件定義書9.2 F4①・KPI#2）。月曜7:00のcronで生成→保存→Canvasメッセージ通知。
  対象週のスナップショットを `weekly_reports` に保存し、再実行は上書き（冪等）。
  未提出課題一覧・停滞アラートを含む（`src/lib/f4/weeklyReport.ts`）。
  通知失敗は生成を失敗させず、理由を記録して画面に表示する。単体17件・E2E9件を追加
- [ ] 実機での「1分以内反映」実証（F4②）〔実機〕

---

## 進め方（推奨順・2026-08-24 更新。詳細監査は docs/実装状況.md）

B（実AI）・D（Jupyter）・E（データ永続化。コード側は2026-08-26完了）は完了。
残りは実受講生データを扱う前に必須なものから：

1. **C-1: F3① 講師の採点結果→Canvas成績表**: 残る唯一の「未達」機能。S7の完了操作が
   Canvasへ書いていない。書き戻し方式（AGS / REST）の判断待ち
2. **F: 脆弱性スキャン**: 個人情報投入前の必須要件
   （DB構築・バックアップ・リストア演習・監査ログのDB権限制御・LTI実起動確認・
   週次レポート自動化は完了）
3. **G/H（実機・負荷・実地トレーニング）**: 9月の機材搬入後
