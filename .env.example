# AI推論基盤の設定（未決事項#3: 【仮】Claude API。抽象化レイヤーで切替可能）
# AI_PROVIDER: mock | claude | local
AI_PROVIDER=mock

# Claude API を使う場合のみ設定（キーはコミット禁止 — CLAUDE.md 2章）
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-4-8

# ローカルLLM（L40S）を使う場合のみ設定
LOCAL_LLM_BASE_URL=
